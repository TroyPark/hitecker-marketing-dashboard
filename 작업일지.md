# 작업 일지 — 2026-09-16

엑셀 한 개(`Ads RAW Data.xlsx`)에서 출발해 Supabase 연동 대시보드 배포까지.

---

## 접속 정보

| 항목 | 값 |
|---|---|
| 사이트 | https://troypark.github.io/hitecker-marketing-dashboard/ |
| 저장소 | https://github.com/TroyPark/hitecker-marketing-dashboard (public) |
| Supabase | https://sxbgkjqqosqexvalyjps.supabase.co |
| 로그인 | `marketing@kfo.or.kr` / `gkdlxpzj11!` (admin) |
| 구역 잠금 | `akzpxld11!` — 미디어 믹스 · 데이터 입력 |

`main` 에 push 하면 GitHub Actions 가 자동 배포한다.

---

## 1. 원천 데이터 분석

`Ads RAW Data.xlsx` 세 시트를 확인했다.

- `SQL값` — 한글명 ↔ 영문코드 매핑 (사업 4 / 플랫폼 8 / 지표 9)
- `(RAW)Learnershigh` — 일별 73컬럼 × 365행, **실데이터 250일** (2026-01-09 ~ 09-15)
- `(RAW)KFO` — 헤더만 있고 값 없음. 헤더 접두사도 `Learnershigh_` 로 잘못 복사돼 있음

컬럼명이 `{사업}_{플랫폼}_{캠페인}_{지표}` 로 규칙적이라 기계 파싱이 가능했다.

### 설계에 영향을 준 발견

1. **CPA · ROAS 컬럼이 전부 비어 있다(`-`)** → 저장하지 않고 조회 시 계산.
   일별 파생지표를 평균내면 틀린 값이 나오므로 항상 기간 합계에서 재계산한다.
2. 미집행일은 `0` 이 아니라 문자열 `-` → 빈 값으로 처리.
3. 73컬럼 wide 구조는 매체 추가마다 컬럼이 9개씩 늘어난다 → long 포맷으로 전환.
4. 2월 이후 효율이 급락(ROAS 120% → 4~15%). 대시보드가 이 추세를 첫 화면에서 보여주게 구성.

---

## 2. 화면

| 탭 | 대상 | 내용 |
|---|---|---|
| 경영 대시보드 | 임원 · CEO | 사업 단위. 매출·결제·가입·광고비·ROAS·CAC, 사업별 매출 추이, 예산 소진, 가입→결제 퍼널, 사업별 성과표 |
| 광고 운영 | 팀장 | 매체 단위. 일별 광고비·전환매출, 매체별 비중, 매체별 성과표, 매체 상세(7일 이동평균 + 퍼널) |
| 미디어 믹스 | 기획 | 총 예산을 매체에 배분 → 월별 예산으로 저장 |
| 데이터 입력 | 운영 | 스프레드시트 입력, 사업·매체 추가, 사업 관리 |

공통: 기간 프리셋(전일/7일/30일/90일/이번달/전체) + 직접 날짜 지정, 라이트·다크 모드, 모든 차트에 표 보기.

### 미디어 믹스
- 비중(%)과 금액 중 아무거나 입력하면 나머지가 따라온다
- 균등 배분 / 전월 집행 비율 버튼
- 총 예산과 배분 합계가 어긋나면 미배분·초과 금액 표시
- 저장하면 `budgets`(월 × 사업 × 매체)로 들어가고 경영 대시보드 예산 소진에 반영

### 데이터 입력
- 사업 탭 → 시트 탭(광고 실적 / 사업 실적 / 월별 예산) → 매체 탭 순으로 좁힌다
- 고른 사업·매체는 칸이 잠기고 새 행도 그 값으로 생성
- 방향키 이동 · Enter 편집 · Tab 다음 칸 · **엑셀 범위 Ctrl+V 붙여넣기**
- 현재 시트 양식대로 CSV 템플릿 내려받기 (엑셀 한글 깨짐 방지 BOM 포함)
- **사업 관리** 표: 종료하면 경영 대시보드에서 빠진다. 데이터는 보존

### 시각화 규칙
- 사업·매체 색은 등록 순서로 고정. 필터로 일부를 빼도 남은 항목 색이 바뀌지 않는다
- 이중축 금지. 단위가 다른 두 지표를 한 차트에 넣지 않는다
- 팔레트는 라이트·다크 양쪽에서 색각이상(protan/deutan) 판별 검증 통과
- 검증된 색은 8칸까지. 9번째부터는 새 색을 만들지 않고 중립 회색 + 직접 라벨

---

## 3. 데이터 모델

| 테이블 | 키 | 내용 |
|---|---|---|
| `brands` / `platforms` | code | 마스터. 화면에서 추가 가능. `brands.active` 로 종료 관리 |
| `ad_daily` | (date, brand, platform) | 노출·클릭·광고비·전환·전환매출 |
| `business_daily` | (date, brand) | 회원가입·결제수·매출 |
| `budgets` | (month, brand, platform) | 매체별 월 예산. 합이 곧 사업 예산 |
| `upload_logs` | id | 업로드 이력 |
| `profiles` | auth.users.id | admin / editor / viewer |
| `section_locks` | section | 구역 잠금 비밀번호 (bcrypt 해시) |

파생지표는 뷰(`v_ad_daily`, `v_brand_daily`)에서 계산한다.

### 보안

GitHub Pages 는 정적 호스팅이라 anon key 가 브라우저에 노출된다. 이건 설계상 정상이고,
데이터를 지키는 건 RLS 다. 추가로 세 가지를 막아 뒀다.

1. **뷰가 RLS를 우회하는 문제** — 뷰는 기본이 소유자 권한이라 테이블에 RLS를 걸어도
   뷰로 조회하면 다 새어 나간다. `security_invoker = on` 으로 막았다.
2. **anon 권한 회수** — 비로그인 역할에서 public 스키마 권한을 전부 회수하고
   로그인 사용자에게만 필요한 만큼 부여. 정책 실수 한 번으로 뚫리지 않게.
3. **구역 잠금 비밀번호** — 평문으로 두면 로그인한 사람이 테이블에서 읽을 수 있다.
   bcrypt 해시만 저장하고 대조는 서버 함수(`verify_section_password`) 안에서 한다.
   `section_locks` 는 로그인 사용자도 직접 읽지 못한다(403 확인).

구역 잠금은 해제 상태를 기억하지 않는다. 다른 메뉴로 나갔다 오면 다시 묻고,
로그아웃하면 경영 대시보드로 초기화된다.

---

## 4. 적재 결과

```
brands           4행   (전부 운영 중)
platforms        8행
ad_daily       911행   내일하이 2026-01-09 ~ 09-15, 광고비 60,182,893원
business_daily   0행   입력 대기
budgets          0행   입력 대기
profiles         1행   marketing@kfo.or.kr (admin)
```

브라우저 실동작 확인 — 로그인 → 911행 로드 → 화면 렌더까지 정상.
최근 30일 광고비 654만원, 메타 58% / 네이버 브랜드검색 23% / 구글 검색광고 16%.
엑셀 값과 일치한다.

---

## 5. 도중에 고친 것

| 문제 | 원인 | 조치 |
|---|---|---|
| 로그인 후에도 로그인 화면이 안 사라짐 | `#login` 에 `display:grid` 가 있어 `hidden` 속성이 무력화됨 | `[hidden] { display:none !important }` |
| 차트 선이 축 위로 삐져나감 | 눈금 최댓값이 데이터 최댓값보다 작을 수 있었음 | `Math.ceil` 로 올림 |
| x축 마지막 라벨끼리 겹침 | 균등 간격 + 마지막 강제 표시 | 46px 이내면 앞 라벨 제거 |
| 미디어 믹스 표가 안 보임 | 광고 운영의 `#mix-plot`·`#mix-table` 과 **ID 충돌** | 믹스 페이지를 `plan-*` 으로 변경 |
| `config.local.js` 가 커밋될 뻔함 | **gitignore 는 줄 끝 주석을 패턴의 일부로 읽는다** | 주석을 별도 줄로 분리 |
| 날짜가 하루씩 밀림 | 로컬 자정을 `toISOString()` 에 넣으면 KST 에서 전날이 됨 | UTC 기준 계산 |
| 예산 소진율이 100% 초과로만 나옴 | 30일 집행액을 두 달치 예산과 비교 | 월 예산을 기간 일수로 일할 계산 |
| 배포 스크립트 파싱 실패 | PowerShell 5.1 이 BOM 없는 UTF-8 `.ps1` 을 ANSI 로 읽음 | BOM 추가 |
| 스크립트가 중간에 끊김 | `ErrorActionPreference=Stop` 에서 native 명령 stderr 가 종료 오류로 처리됨 | 종료 코드로 판단 |
| git push 인증 실패 | git 이 gh 자격증명을 쓰지 않음 | `gh auth setup-git` |
| private 레포 Pages 배포 거부 | 무료 플랜 제한 | 확인 후 public 전환 |

### 임의 데이터를 넣었다가 되돌린 건

처음에 KFO·오라클·K-뉴딜의 광고 데이터와 4개 사업의 예산·가입·결제·매출을
화면 확인용으로 생성해 넣었다. 같은 사업의 전환매출(747,560원)과 매출(2,730,534원)이
어긋나 보여 지적받았고, 전부 걷어내고 **실데이터 911행만** 남겼다.
없는 값은 `–` 와 "데이터 없음"으로 표시한다.

---

## 6. 아직 안 된 것

- **엑셀 드래그&드롭 업로드** — 화면만 있고 실제 xlsx 파싱은 미구현.
  대량 적재는 `scripts/migrate.py` 로 처리한다.
- **업로드 이력** — 하드코딩된 3줄. `upload_logs` 와 연결 필요.
- **사업 실적 · 예산 데이터** — 아직 0행. 데이터 입력 탭에서 채워야 한다.
- KFO · 오라클 · K-뉴딜 광고 데이터 확보

---

## 7. 자주 쓰는 명령

```powershell
# 엑셀 → Supabase 적재
python scripts/migrate.py "Ads RAW Data.xlsx" --dry-run   # 검증만
python scripts/migrate.py "Ads RAW Data.xlsx"

# 구역 잠금 비밀번호 변경
python scripts/set_lock.py mix "새비밀번호"
python scripts/set_lock.py input "새비밀번호"

# 배포 (평소에는 git push 만 하면 자동 배포)
powershell -ExecutionPolicy Bypass -File scripts\deploy.ps1
```

로컬에서 볼 때는 `index.html` 을 더블클릭한다.
`config.js` 가 있으면 Supabase 에 붙고, 없으면 `data.js` 의 로컬 데이터로 동작한다.

### 커밋에 올리지 않는 것
`.env` · `config.js` · `data.js` · `Ads RAW Data.xlsx` · `prototype_data.json`
(배포된 사이트에서 404 인 것으로 확인 완료)
